<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/splashFragment">

    <fragment
        android:id="@+id/permissionFragment"
        android:name="com.example.myapplication.ui.permission.PermissionFragment"
        android:label="Permission">
        <action
            android:id="@+id/action_permission_to_map"
            app:destination="@id/mapFragment"
            app:popUpTo="@id/permissionFragment"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.example.myapplication.ui.map.MapFragment"
        android:label="Map"/>
    <fragment
        android:id="@+id/splashFragment"
        android:name="com.example.myapplication.ui.splash.SplashFragment"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" >
        <action
            android:id="@+id/action_splashFragment_to_permissionFragment"
            app:destination="@id/permissionFragment"
            app:launchSingleTop="false"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />
    </fragment>
</navigation>
